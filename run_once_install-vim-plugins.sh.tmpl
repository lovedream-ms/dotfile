#!/bin/bash

# 1. 自动下载 vim-plug (如果不存在)
if [ ! -f "${HOME}/.vim/autoload/plug.vim" ]; then
    curl -fLo "${HOME}/.vim/autoload/plug.vim" --create-dirs \
        https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
fi

# 2. 调用 vim 命令行安装插件并退出
# -c 参数让 vim 执行命令，'qa!' 确保安装完自动退出
vim +PlugInstall +qa!
